# Faberon Deployment Guide

## 🚀 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     VERCEL (Frontend)                       │
│  https://faberon.vercel.app                                 │
│  - HTML/CSS/JavaScript                                      │
│  - Static hosting                                           │
│  - Global CDN                                               │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ API Calls
                       ↓
┌─────────────────────────────────────────────────────────────┐
│              LOCAL DOCKER (Backend)                         │
│  http://your-ip:8080 (UserService)                          │
│  http://your-ip:8081 (SellerService)                        │
│  http://your-ip:8082 (PriceService)                         │
│  - PostgreSQL Database                                      │
│  - Go Microservices                                         │
│  - Docker Compose                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 ЧАСТЬ 1: BACKEND (Docker - Локально)

### Требования

- Docker & Docker Compose
- 3 свободных порта: 8080, 8081, 8082
- PostgreSQL (в Docker)

### Шаг 1: Подготовка Backend

```bash
# Перейти в директорию UserService
cd /Users/yaroslav/Desktop/Faberon/SMC-UserService-main

# Проверить docker-compose.yml
cat docker-compose.yml

# Убедиться, что все сервисы настроены
```

### Шаг 2: Запуск Backend через Docker

```bash
# Остановить все контейнеры (если они запущены)
docker-compose down

# Пересобрать образы (если нужны изменения)
docker-compose up -d --build

# Проверить статус
docker-compose ps

# Проверить логи
docker-compose logs -f app
```

### Шаг 3: Проверить Backend доступность

```bash
# Проверить UserService
curl -X GET http://localhost:8080/users/me \
  -H "X-User-ID: 123456789"

# Проверить SellerService
curl -X GET http://localhost:8081/api/v1/companies

# Проверить PriceService
curl -X POST http://localhost:8082/api/v1/prices/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "company_id": 1,
    "service_ids": [106],
    "user_id": 123456789
  }'
```

### Шаг 4: Получить IP адрес компьютера

```bash
# macOS
ifconfig | grep "inet " | grep -v 127.0.0.1

# Linux
hostname -I

# Windows
ipconfig
```

**Запомните IP адрес!** Например: `192.168.1.100`

### Шаг 5: Обновить CORS в Backend (если нужно)

Убедитесь, что CORS настроен на все домены:

```go
// SMC-PriceService-main/cmd/main_cors.go
w.Header().Set("Access-Control-Allow-Origin", "*")
```

---

## 🌐 ЧАСТЬ 2: FRONTEND (Vercel)

### Требования

- GitHub аккаунт
- Vercel аккаунт (бесплатно на vercel.com)
- Git установлен

### Шаг 1: Подготовить Git репозиторий

```bash
# Перейти в директорию фронтенда
cd /Users/yaroslav/Desktop/Faberon

# Убедиться, что репозиторий инициализирован
git status

# Если нет, инициализировать
git init
git remote add origin https://github.com/Notiberg/faberon24.git

# Добавить все файлы
git add -A

# Коммитить
git commit -m "Prepare for Vercel deployment"

# Загрузить на GitHub
git push origin main
```

### Шаг 2: Подключить Vercel к GitHub

1. Перейти на https://vercel.com
2. Нажать **"New Project"**
3. Выбрать **"Import Git Repository"**
4. Выбрать репозиторий `faberon24`
5. Нажать **"Import"**

### Шаг 3: Настроить Environment Variables в Vercel

В Vercel Dashboard:

1. Перейти в **Settings** → **Environment Variables**
2. Добавить переменные:

```
API_BASE_URL = http://YOUR_IP:8080
PRICE_API_BASE = http://YOUR_IP:8082/api/v1
SELLER_API_BASE = http://YOUR_IP:8081/api/v1
```

**Замените `YOUR_IP` на ваш IP адрес!** (например: `192.168.1.100`)

### Шаг 4: Обновить API URLs в коде (опционально)

Если хотите жестко закодировать URLs:

**js/api.js:**
```javascript
const API_BASE_URL = 'http://YOUR_IP:8080';
```

**js/seller.js:**
```javascript
const PRICE_API_BASE = 'http://YOUR_IP:8082/api/v1';
const SELLER_API_BASE = 'http://YOUR_IP:8081/api/v1';
```

### Шаг 5: Развернуть на Vercel

```bash
# Убедиться, что все файлы закоммичены
git status

# Загрузить на GitHub
git push origin main

# Vercel автоматически развернет!
# Проверить статус на https://vercel.com/dashboard
```

### Шаг 6: Получить Vercel URL

После развертывания:

1. Перейти на https://vercel.com/dashboard
2. Найти проект `faberon24`
3. Скопировать URL (например: `https://faberon24.vercel.app`)

---

## 🔗 ИНТЕГРАЦИЯ: Frontend + Backend

### Проблема: CORS при обращении с Vercel к локальному Backend

**Решение 1: Использовать ngrok (туннель)**

```bash
# Установить ngrok
brew install ngrok

# Запустить туннель для порта 8080
ngrok http 8080

# Скопировать URL (например: https://abc123.ngrok.io)

# Обновить API_BASE_URL в Vercel:
# API_BASE_URL = https://abc123.ngrok.io
```

**Решение 2: Использовать VPN/Proxy**

Если у вас есть VPN или прокси, который доступен из интернета.

**Решение 3: Развернуть Backend на облаке**

Развернуть Backend на AWS, DigitalOcean или другом облачном сервисе.

### Проверить интеграцию

1. Открыть Vercel URL в браузере
2. Добавить параметр: `?X-UserID=123456789`
3. Проверить, что загружаются данные пользователя
4. Открыть DevTools (F12) → Network
5. Проверить, что API запросы идут на Backend

---

## 📝 ПОЛНЫЙ ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

### Backend (Docker)

- [ ] Docker & Docker Compose установлены
- [ ] Все 3 сервиса запущены (UserService, SellerService, PriceService)
- [ ] PostgreSQL работает
- [ ] API доступны на портах 8080, 8081, 8082
- [ ] CORS настроен
- [ ] Получен IP адрес компьютера

### Frontend (Vercel)

- [ ] GitHub репозиторий обновлен
- [ ] vercel.json создан
- [ ] Vercel проект создан
- [ ] Environment Variables настроены
- [ ] Развертывание успешно
- [ ] Vercel URL получен

### Интеграция

- [ ] Frontend может обращаться к Backend
- [ ] CORS ошибок нет
- [ ] Данные пользователя загружаются
- [ ] Машины отображаются
- [ ] Услуги загружаются
- [ ] Цены рассчитываются

---

## 🧪 ТЕСТИРОВАНИЕ

### Локальное тестирование

```bash
# 1. Запустить Backend
cd SMC-UserService-main
docker-compose up -d

# 2. Запустить Frontend локально
cd ..
python3 -m http.server 8000

# 3. Открыть в браузере
open http://localhost:8000/index.html?X-UserID=123456789
```

### Production тестирование

```bash
# 1. Убедиться, что Backend запущен
docker-compose ps

# 2. Открыть Vercel URL
https://faberon24.vercel.app?X-UserID=123456789

# 3. Проверить в DevTools
# - Network: API запросы идут на Backend
# - Console: Нет ошибок
# - Application: localStorage содержит userID
```

---

## 🐛 TROUBLESHOOTING

### Frontend не может подключиться к Backend

**Проблема**: CORS ошибка или "Failed to fetch"

**Решения**:
1. Проверить, что Backend запущен: `docker-compose ps`
2. Проверить IP адрес: `ifconfig`
3. Проверить firewall: разрешить порты 8080, 8081, 8082
4. Использовать ngrok для туннеля
5. Проверить CORS в Backend

### Vercel развертывание не работает

**Проблема**: Build ошибка или 404

**Решения**:
1. Проверить логи: Vercel Dashboard → Deployments
2. Убедиться, что vercel.json правильный
3. Проверить, что все файлы закоммичены в Git
4. Пересоздать проект на Vercel

### API возвращает 400 или 401

**Проблема**: Неправильный формат запроса

**Решения**:
1. Проверить заголовки: `X-User-ID` должен быть числом
2. Проверить body запроса: JSON должен быть валидным
3. Проверить Backend логи: `docker-compose logs app`

---

## 📊 МОНИТОРИНГ

### Проверить Backend здоровье

```bash
# Проверить контейнеры
docker-compose ps

# Проверить логи
docker-compose logs -f

# Проверить использование ресурсов
docker stats

# Проверить сеть
docker network ls
```

### Проверить Frontend здоровье

1. Vercel Dashboard → Project → Deployments
2. Проверить статус последнего развертывания
3. Проверить Analytics
4. Проверить Logs

---

## 🔐 БЕЗОПАСНОСТЬ

### Перед production

- [ ] Обновить API URLs на production домены
- [ ] Включить HTTPS везде
- [ ] Настроить правильные CORS headers
- [ ] Добавить аутентификацию (JWT)
- [ ] Добавить rate limiting
- [ ] Включить WAF (Web Application Firewall)
- [ ] Регулярно обновлять зависимости

---

## 📞 ПОДДЕРЖКА

Если возникают проблемы:

1. Проверить логи Backend: `docker-compose logs`
2. Проверить логи Frontend: DevTools Console
3. Проверить Network запросы: DevTools Network
4. Проверить GitHub Issues
5. Создать новый Issue с описанием проблемы

---

**Last Updated**: December 10, 2025
**Version**: 1.0
