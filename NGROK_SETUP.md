# 🔗 NGROK SETUP - Подключение Vercel к локальному Backend

## ⚠️ ПРОБЛЕМА

Vercel (облако) не может подключиться к локальному Backend (192.168.3.26). Нужно создать туннель через **ngrok**.

---

## ✅ РЕШЕНИЕ: Использовать ngrok

### Шаг 1: Скачать ngrok

Перейти на https://ngrok.com/download и скачать для macOS

Или через терминал:

```bash
cd /tmp
curl -s https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-darwin-amd64.zip -o ngrok.zip
unzip ngrok.zip
chmod +x ngrok
```

### Шаг 2: Запустить ngrok туннель для UserService

```bash
/tmp/ngrok http 8080
```

Вы получите URL вроде:
```
https://abc123-xyz789.ngrok.io
```

**Скопируйте этот URL!**

### Шаг 3: Обновить Vercel Environment Variables

1. Перейти на https://vercel.com/dashboard
2. Выбрать проект `faberon24`
3. Перейти в **Settings** → **Environment Variables**
4. Обновить переменные:

```
API_BASE_URL = https://abc123-xyz789.ngrok.io
PRICE_API_BASE = https://abc123-xyz789.ngrok.io:8082/api/v1
SELLER_API_BASE = https://abc123-xyz789.ngrok.io:8081/api/v1
```

**Замените `abc123-xyz789` на ваш ngrok URL!**

### Шаг 4: Redeploy на Vercel

1. Перейти в **Deployments**
2. Нажать на последнее развертывание
3. Нажать **"Redeploy"**
4. Дождаться завершения

### Шаг 5: Тестировать

```
https://faberon24.vercel.app?X-UserID=123456789
```

Должны загрузиться:
- ✅ Машины пользователя
- ✅ Услуги
- ✅ Цены

---

## 🚀 АВТОМАТИЗИРОВАННЫЙ СПОСОБ

Если хотите запустить все туннели сразу:

```bash
# Запустить UserService туннель
/tmp/ngrok http 8080 &

# В другом терминале - SellerService туннель
/tmp/ngrok http 8081 &

# В третьем терминале - PriceService туннель
/tmp/ngrok http 8082 &
```

Затем скопируйте все 3 URL в Vercel Environment Variables.

---

## 📊 NGROK URLS

После запуска ngrok, проверить URLs можно на:

```
http://localhost:4040
```

Там будут все активные туннели с их публичными URL.

---

## ⚙️ АЛЬТЕРНАТИВА: Использовать другой туннель

Если ngrok не подходит, можно использовать:

- **Cloudflare Tunnel** (бесплатно)
- **Localtunnel** (бесплатно)
- **Expose** (платно)
- **Serveo** (бесплатно)

---

## 🔄 ОБНОВЛЕНИЕ NGROK URL

Если ngrok URL изменился (например, при перезагрузке):

1. Запустить ngrok заново
2. Скопировать новый URL
3. Обновить в Vercel Environment Variables
4. Нажать **"Redeploy"**

---

## ✅ ГОТОВО!

Теперь Vercel может подключаться к локальному Backend через ngrok туннель! 🎉

---

**Last Updated**: December 10, 2025
